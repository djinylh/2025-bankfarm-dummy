<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace = "com.bankfarm_dummy.bankfarm_dummy.card.CheckCardMapper">


    <insert id="checkCardJoin">

        INSERT IGNORE INTO check_card
        SET
        card_user_id = #{cardUserId}
        ,card_acct_id = #{cardAcctId}

    </insert>

    <select id="checkCardList">
        SELECT uc.card_user_id
        FROM user_card uc
        LEFT JOIN card c
        ON c.card_id = uc.card_id
        WHERE c.card_tp = 1
    </select>

</mapper>